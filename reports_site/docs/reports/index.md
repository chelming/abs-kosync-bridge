# Reports

One page per finding, added by the parser as `YYYY-MM-DD_<finding-id>.md`
(date = first seen). Each page follows the shared template: instance UUID,
enabled integrations, error context, verbatim logs, and a developer response
block that the parser never overwrites.
